# Minecraft Marketplace - devenv configuration
# Uses devenv.sh for complete development environment orchestration

inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-unstable
  devenv:
    url: github:cachix/devenv

allowUnfree: true

imports:
  - ./nix/devenv.nix